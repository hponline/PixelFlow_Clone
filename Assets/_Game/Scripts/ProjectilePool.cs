using Lean.Pool;
using UnityEngine;

public class ProjectilePool : MonoBehaviour
{
    public ProjectileData projectileDataSO;

    void Start()
    {
        //LeanPool.lo(projectileData.prefab, projectileData.preloadCount);
    }
}
